{
  "name": "node-js",
  "version": "1.0.0",
  "description": "Estudo da clean Architecture.",
  "main": "index.js",
  "scripts": {
    "test": "jest --silent --passWithNoTests --colors --noStackTrace",
    "test:unit": " npm test -- --watch -c jest-unit-config.js",
    "test:integration": " npm test -- --watch -c jest-integration-config.js",
    "test:staged": "npm test -- --findRelatedTests",
    "test:ci": "npm test -- --coverage"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "keywords": [],
  "author": "João Vitor",
  "license": "ISC",
  "devDependencies": {
    "husky": "^4.3.8",
    "jest": "^27.4.7",
    "lint-staged": "^12.3.3",
    "standard": "^16.0.4"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "jsonwebtoken": "^8.5.1",
    "validator": "^13.7.0"
  }
}
